[project]
name = "ebook-factory-workflow-engine"
version = "0.1.0"
description = "LangGraph state machine with HITL"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2.0",
    "pydantic>=2.9.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.0",
    "ruff>=0.6.0",
]
